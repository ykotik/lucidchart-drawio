# swimlanes-trust-zones

Build a horizontal swimlanes diagram with 4 trust zones:
1. Public (DMZ) — green
2. Application — neutral
3. Data — amber (auth gap to flag)
4. Partner / External — purple dashed

Show 3 shapes per lane with edges between adjacent lanes.

Use the swimlanes pattern.
