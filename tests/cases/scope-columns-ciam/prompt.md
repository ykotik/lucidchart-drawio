# scope-columns-ciam

Create a diagram with two scope columns: Internal Platform (green boundary) containing Identity Platform, User Store, Policy Engine, Consent Manager, Session Service, Audit Service, Admin Portal, and Analytics Dashboard; External Integrations (black boundary) containing Okta, Auth0 CIAM, Twilio Verify, SendGrid, Stripe Identity, Experian KYC, and HubSpot CRM.

Use the scope-columns pattern.
