# pipeline-etl-warehouse

Create an AWS data warehouse ETL pipeline: Sources (PostgreSQL, MySQL, Salesforce API, S3 Raw) -> AWS Glue Crawlers + Glue ETL Jobs -> S3 Staging -> Redshift Cluster -> QuickSight Dashboards + SageMaker ML models.

Use the pipeline pattern.
