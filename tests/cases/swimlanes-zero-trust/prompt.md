# swimlanes-zero-trust

Create a Zero-Trust network swimlanes diagram with 4 zones (Internet, DMZ, Internal Network, Data Center). Show elements like WAF, API Gateway, Auth Service, DB Cluster, Cache, Vault, and Vault Config across the lanes.

Use the swimlanes pattern.
