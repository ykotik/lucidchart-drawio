# flowchart-dag-ci

Create a CI/CD pipeline flowchart containing: Start, Git Push, Lint Check, lint-pass decision, Unit Tests, Integration Tests, tests-pass decision, Build Docker Image, Push to Registry, Deploy Staging, Smoke Tests, smoke-pass decision, Deploy Production, Health Check, and End. Connect failure loops back.

Use the flowchart-dag pattern.
